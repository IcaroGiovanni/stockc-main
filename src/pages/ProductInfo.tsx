import '../../public/css/estoque-moderno.css';

export default function ProductInfo() {
  return (
    <div
      className="product-modern-page"
      style={{
        minHeight: '100vh',
        width: '100vw',
        background: 'var(--color-background, #0D0D0D)',
        color: 'var(--color-text, #E0E0E0)',
        overflowX: 'hidden',
        boxSizing: 'border-box',
      }}
    >
      {/* Conteúdo da página do produto */}
    </div>
  );
} 